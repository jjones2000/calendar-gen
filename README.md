# README

This is a simple python script that creates a file to be used for daily org mode journalling.
It creates indented days and months to allow tab folding.

The user just needs to input the desired year at the top and run the script. It will the create a text file.
